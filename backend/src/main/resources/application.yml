spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codecollab
    username: postgres
    password: postgres123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:codecollabsecretkeythatisatleast256bitslong1234567890abcdef}
  expiration: 86400000



