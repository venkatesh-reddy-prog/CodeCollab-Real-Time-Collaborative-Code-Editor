server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codecollab
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

jwt:
  secret: mysecretkey123
  expiration: 86400000